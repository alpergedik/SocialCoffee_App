import 'package:flutter/material.dart';

class AppColors {
  // Mevcut Renklerin
  static const Color primaryBrown = Color(0xFF2D1B14);
  static const Color darkButton = Color(0xFF1D0F0A);
  static const Color accentOrange = Color(0xFFE4A972);
  static const Color cardWhite = Colors.white;

  // --- Rapor Ekranı İçin Eklenen Renkler ---
  static const Color textDark = Color(0xFF1D0F0A);
  static const Color textGray = Color(0xFF757575);
  static const Color background = Color(0xFFF5F5F5);

  // Nakit / Başarılı (Yeşil)
  static const Color successGreen = Color(0xFF2E7D32);
  static const Color successBg = Color(0xFFE8F5E9);
  
  // Kredi Kartı / Bilgi (Mavi)
  static const Color infoBlue = Color(0xFF1565C0);
  static const Color infoBg = Color(0xFFE3F2FD);
  
  // İade / Hata (Kırmızı)
  static const Color errorRed = Color(0xFFC62828);
  static const Color errorBg = Color(0xFFFFEBEE);
}